# Ochyai_Note（一次資料）

落合陽一 note 記事のアーカイブ。

## 構成
- `articles/*.md`：本文（LLMはこれを優先）
- `articles/*.json`：メタデータ + 画像のキャプション/説明
- `images/`：本文内で参照される画像
- `scripts/`：スクレイピング用（必要時のみ）

## 使い方（LLM）
- まず `../LLM/02_Ochyai_Note_insights.md` を読む（要約と論点）。
- 原文確認は `articles/2025-12-21_...md` など必要箇所だけを追加。

