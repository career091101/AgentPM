# Post LinkedIn to Threads Skill

LinkedIn投稿（1,150-1,300字）をThreadsスレッド形式（500字×3投稿）に変換してLate API経由で予約投稿するスキル。

---

## このSkillでできること

1. **Markdown装飾の除去**: `**太字**`、`- 箇条書き`等を除去
2. **セクション区切りの自動挿入**: 主要セクション前に`━━━`を挿入
3. **500字×3投稿への自動分割**: `split_for_threads()`ロジックで最適分割
4. **続きマーカー追加**: 投稿1-2の末尾に`👇続く`を追加
5. **Late API予約投稿**: Threadsアカウントに自動投稿

---

## 入力・出力

| 項目 | 内容 |
|------|------|
| **入力** | LinkedIn投稿本文（1,150-1,300字、高野式7パターン準拠） |
| **出力** | Threadsスレッド予約投稿（3投稿、Late API経由） |
| **予約時刻** | 翌利用可能日 20:00 JST |

---

## Instructions

**実行モード**: ClaudeCode LLM自律実行（スクリプト不使用）
**推定所要時間**: 2-3分

### STEP 1: LinkedIn投稿の読み込み（30秒）

**入力ファイル**:
```
Stock/programs/副業/projects/SNS/data/posts_generated_takano_{最新日付}.md
```

**読み込み内容**:
- 案1、案2、案3のいずれか（ユーザー指定、デフォルトは案2）
- 本文のみを抽出（タイトル、First Commentは除外）

---

### STEP 2: Markdown装飾の除去（30秒）

**処理内容**:

1. **太字除去**: `**テキスト**` → `テキスト`
   ```python
   import re
   content = re.sub(r'\*\*(.+?)\*\*', r'\1', content)
   ```

2. **箇条書き除去**: `- 項目` → `項目`
   ```python
   content = re.sub(r'^\- ', '', content, flags=re.MULTILINE)
   ```

3. **番号付きリスト除去**: `1. 項目` → `項目`
   ```python
   content = re.sub(r'^\d+\. ', '', content, flags=re.MULTILINE)
   ```

---

### STEP 3: セクション区切りの挿入（30秒）

**対象セクション**（案2の例）:
- `投資規模の異常性:`
- `循環投資の仕組み:`
- `OpenAIの財務状況の実態:`

**処理**:
```python
content = re.sub(
    r'\n(投資規模の異常性|循環投資の仕組み|OpenAIの財務状況の実態):',
    r'\n━━━\n\1:',
    content
)
```

**注意**: セクション名は案によって異なるため、以下のパターンで自動検出推奨:
- `**見出し**:` → `━━━\n見出し:`
- または手動で主要セクションを指定

---

### STEP 4: Threadsスレッド分割（30秒）

**分割ロジック** (`split_for_threads()`を模倣):

1. `━━━`でセクション分割
2. 各セクションが500字以内なら結合
3. 500字超過の場合は段落（`\n\n`）で分割
4. 最終的に3投稿に調整

**期待結果**:
- 投稿1: 400-500字
- 投稿2: 400-500字
- 投稿3: 400-500字

---

### STEP 5: 続きマーカーの追加（10秒）

**処理**:
```python
# 投稿1の末尾に追加
posts[0] += '\n\n👇続く'

# 投稿2の末尾に追加
posts[1] += '\n\n👇続く'

# 投稿3は最終投稿なので追加しない
```

---

### STEP 6: Late API予約投稿（30秒）

**Threadsアカウント取得**:
```python
from scripts.late_api_post import get_account_id
threads_account_id = get_account_id('threads')
```

**利用可能日付検索**:
```python
from scripts.late_api_post import find_available_dates
date_info = find_available_dates(count=1)
available_date = date_info['available_dates'][0]
```

**予約投稿実行**:
```python
import requests
from datetime import datetime
from zoneinfo import ZoneInfo

# 設定読み込み
with open('config/late_api_config.json', 'r') as f:
    config = json.load(f)

api_key = config['api_key']
base_url = config['base_url']

# 予約日時: 翌利用可能日 20:00 JST
jst = ZoneInfo('Asia/Tokyo')
scheduled_datetime = datetime.combine(
    available_date,
    datetime.min.time()
).replace(hour=20, minute=0, second=0, tzinfo=jst)

# threadItems形式に変換
thread_items_data = [{'content': post} for post in posts]

# リクエストボディ
request_body = {
    'content': posts[0],  # 最初の投稿を必ず設定（Late API仕様）
    'platforms': [{
        'platform': 'threads',
        'accountId': threads_account_id,
        'platformSpecificData': {'threadItems': thread_items_data}
    }],
    'scheduledFor': scheduled_datetime.isoformat(),
    'timezone': 'Asia/Tokyo'
}

# API呼び出し
response = requests.post(
    f'{base_url}/posts',
    headers={
        'Authorization': f'Bearer {api_key}',
        'Content-Type': 'application/json'
    },
    json=request_body,
    timeout=30
)

if response.status_code in [200, 201]:
    result = response.json()
    post_id = result['post']['_id']
    print(f'✅ Threadsスレッド予約完了')
    print(f'   Post ID: {post_id}')
    print(f'   URL: https://app.getlate.dev/posts/{post_id}')
    print(f'   予約日時: {scheduled_datetime.strftime("%Y-%m-%d %H:%M JST")}')
else:
    print(f'❌ エラー ({response.status_code}): {response.text}')
```

---

## エラーハンドリング

### LinkedIn投稿ファイルが見つからない場合
- **対応**: 最新の`posts_generated_takano_*.md`を検索
- **失敗時**: ユーザーに入力ファイルパスを確認

### セクション区切り挿入失敗
- **原因**: セクション名が予測と異なる
- **対応**: 手動で`━━━`を挿入する箇所をユーザーに確認

### 500字超過エラー
- **原因**: 分割ロジックが期待通り動作しない
- **対応**: 段落（`\n\n`）を追加して分割箇所を調整

### Late API 400エラー
- **原因**: `content`フィールドが空または不正
- **対応**: `content`に`posts[0]`が正しく設定されているか確認

### Late API 401エラー
- **原因**: API Key無効
- **対応**: `config/late_api_config.json`のAPI Keyを確認

---

## 使用例

### 基本的な使用

```
User: LinkedIn投稿をThreadsに投稿して
```

システムが自動的に：
1. 最新の`posts_generated_takano_*.md`を読み込み
2. 案2（最推奨案）を抽出
3. Markdown除去 → セクション区切り → 分割 → 続きマーカー追加
4. Threads予約投稿（翌利用可能日 20:00 JST）

### カスタムパラメータ使用

```
User: LinkedIn投稿の案1をThreadsに投稿して
```

システムが自動的に：
- 案1を抽出して同様の処理

---

## 依存ツール

**必須**:
- `Read`: 入力ファイル読み込み
- `Bash`: Python実行（Late API呼び出し）

**Python依存**:
- `requests`: Late API HTTP通信
- `json`: 設定ファイル読み込み
- `datetime`, `zoneinfo`: 日時処理
- `re`: 正規表現処理

---

## 次のアクション提案

投稿完了後、以下のアクションを提案します：

1. **Late APIダッシュボード確認**: https://app.getlate.dev/dashboard
2. **投稿プレビュー**: 各投稿の文字数・内容確認
3. **予約時刻変更**: 必要に応じて時刻調整

---

## 技術的詳細

### Late API threadItems仕様

**重要**: `threadItems`使用時も`content`フィールドが必須

```json
{
  "content": "投稿1の内容",  // 必須（threadItems[0].contentと同じ）
  "platforms": [{
    "platform": "threads",
    "accountId": "...",
    "platformSpecificData": {
      "threadItems": [
        {"content": "投稿1の内容"},
        {"content": "投稿2の内容"},
        {"content": "投稿3の内容"}
      ]
    }
  }],
  "scheduledFor": "2026-01-13T20:00:00+09:00",
  "timezone": "Asia/Tokyo"
}
```

### 続きマーカーの効果

- **視認性向上**: ユーザーがスレッドの続きがあることを即座に認識
- **エンゲージメント向上**: 「続きが気になる」心理で読了率アップ
- **Threads最適化**: 絵文字使用でプラットフォーム特性に適合

---

## 更新履歴

- 2026-01-07: 初版作成（ClaudeCode LLM推論型）
- 実績: 案2（OpenAI×NVIDIA）を3投稿に分割、Late API予約投稿成功
