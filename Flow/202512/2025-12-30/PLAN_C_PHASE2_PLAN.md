# プランC: Phase2詳細計画書

**作成日時**: 2025-12-30 19:30
**フェーズ**: Phase2 - アダプタ層構築
**破壊リスク**: **0%** (引き続き aipm_v0_dev/ 内で作業)

---

## 📋 Phase2の目的

Phase1で構築したSSoT層（Single Source of Truth）を基盤として、各AIツール用のアダプタ層を実装します。

### 主要目標

1. **ツール固有アダプタの実装**
   - Antigravity用アダプタ
   - Claude Code用アダプタ
   - Codex用アダプタ

2. **共通コアの活用**
   - Phase1のコンポーネント（trigger_router, path_resolver, template_engine）を再利用
   - ツール固有の設定のみをアダプタ層で処理

3. **100%テストカバレッジ維持**
   - 各アダプタの単体テスト
   - 統合テスト

4. **既存システムへの影響ゼロ**
   - 全て aipm_v0_dev/ 内で作業
   - aipm_v0/ への変更なし

---

## 🏗️ アーキテクチャ設計

### レイヤー構成

```
┌─────────────────────────────────────────────┐
│  ツール層（既存）                              │
│  .agent/  .antigravity/  .claude/  .codex/  │
└─────────────────┬───────────────────────────┘
                  │ 参照（読み取り専用）
┌─────────────────▼───────────────────────────┐
│  アダプタ層（Phase2で実装）                    │
│  antigravity_adapter.py                     │
│  claude_adapter.py                          │
│  codex_adapter.py                           │
└─────────────────┬───────────────────────────┘
                  │ 使用
┌─────────────────▼───────────────────────────┐
│  SSOT層（Phase1で実装済み）                   │
│  trigger_router.py                          │
│  path_resolver.py                           │
│  template_engine.py                         │
└─────────────────────────────────────────────┘
```

### データフロー

```
ユーザー入力（トリガーワード）
    ↓
アダプタ層（ツール固有設定読み込み）
    ↓
TriggerRouter（ルール決定）
    ↓
PathResolver（パス解決）
    ↓
TemplateEngine（文書生成）
    ↓
出力ファイル
```

---

## 📦 実装コンポーネント詳細

### 1. antigravity_adapter.py

**目的**: Antigravity用のインターフェース

**主要機能**:
- `.agent/` と `.antigravity/` フォルダの設定読み込み
- Antigravity固有のトリガーワード処理
- Antigravity向けワークフロー実行

**読み込む設定**:
- `.antigravity/workflows/*.md` - 基本PMBOKワークフロー（9ファイル）
- `.agent/workflows/*.md` - 拡張ビジネスワークフロー（45ファイル）
- `.agent/rules/daily_tasks.md` - タスク管理設定

**実装サイズ**: 約250-300行

**テスト数**: 12-15テスト

---

### 2. claude_adapter.py

**目的**: Claude Code用のインターフェース

**主要機能**:
- `.claude/` フォルダの設定読み込み
- Claude Code固有のスキルシステム対応
- Ultra-lightタスク管理対応

**読み込む設定**:
- `.claude/rules/*.md` - PMBOKルール（13ファイル）
- `.claude/commands/*.md` - コマンド定義（10ファイル）
- `.claude/skills/` - スキル定義（47+ファイル）
- `.claude/daily/` - デイリータスク設定（3ファイル）

**実装サイズ**: 約300-350行

**テスト数**: 15-18テスト

---

### 3. codex_adapter.py

**目的**: Codex用のインターフェース

**主要機能**:
- `.codex/` フォルダの設定読み込み
- Codex固有のスキルインデックス処理
- `.cursor/` への参照処理

**読み込む設定**:
- `.codex/skills/*/SKILL.md` - スキル定義（12ファイル）
- 各スキルから `.cursor/rules/basic/*.mdc` への参照

**実装サイズ**: 約200-250行

**テスト数**: 10-12テスト

---

## 🧪 テスト戦略

### 単体テスト（各アダプタ）

**テストケース例**:
1. 設定ファイル読み込みテスト
2. トリガーワード解決テスト
3. ツール固有設定の適用テスト
4. エラーハンドリングテスト
5. パス解決の統合テスト
6. テンプレート生成の統合テスト

### 統合テスト

**テストシナリオ**:
1. **エンドツーエンドフロー**
   - トリガーワード入力 → 文書生成まで
   - 各ツールでの動作確認

2. **クロスツールテスト**
   - 同じトリガーワードで3つのアダプタが正しく動作するか
   - 出力の一貫性確認

3. **設定変更対応テスト**
   - ツール固有設定を変更した時の動作確認

---

## 📁 ディレクトリ構造

Phase2完了後の構造:

```
aipm_v0_dev/
├── docs/ai/lib/
│   ├── core/                      ← Phase1完成
│   │   ├── trigger_router.py
│   │   ├── path_resolver.py
│   │   └── template_engine.py
│   └── adapters/                  ← Phase2で作成
│       ├── __init__.py
│       ├── antigravity_adapter.py
│       ├── claude_adapter.py
│       └── codex_adapter.py
├── tests/
│   ├── unit/                      ← Phase1完成
│   │   ├── test_trigger_router.py
│   │   ├── test_path_resolver.py
│   │   └── test_template_engine.py
│   ├── integration/               ← Phase2で作成
│   │   ├── test_antigravity_adapter.py
│   │   ├── test_claude_adapter.py
│   │   └── test_codex_adapter.py
│   └── e2e/                       ← Phase2で作成
│       └── test_full_workflow.py
└── config/                        ← Phase2で作成
    ├── antigravity_config.yaml
    ├── claude_config.yaml
    └── codex_config.yaml
```

---

## 📊 実装スケジュール

### 推定所要時間

| タスク | 予定時間 | 実装 | テスト |
|--------|---------|------|--------|
| antigravity_adapter.py | 1.5時間 | 1時間 | 30分 |
| claude_adapter.py | 2時間 | 1.5時間 | 30分 |
| codex_adapter.py | 1時間 | 40分 | 20分 |
| 統合テスト | 1時間 | 40分 | 20分 |
| Phase2レポート | 30分 | 30分 | - |
| **合計** | **6時間** | **4.5時間** | **1.5時間** |

※ Phase1の実績から、実際の所要時間は予定を大幅に短縮できる見込み

---

## 🛡️ リスク管理

### リスク評価

| リスク | 確率 | 影響度 | 対策 |
|--------|------|--------|------|
| 既存システムへの影響 | 極小 | 高 | aipm_v0_dev/内で完全分離 |
| テストの失敗 | 低 | 中 | Phase1同様のTDD手法 |
| 設定ファイル互換性 | 低 | 中 | 読み取り専用で参照 |
| 実装時間超過 | 中 | 低 | 段階的実装で調整可能 |

### 安全性確保

✅ **既存システム保護**:
- aipm_v0/ への変更なし
- 読み取り専用アクセスのみ

✅ **バックアップ**:
- Phase0のバックアップ継続有効
- `pre-restructure-20251230_180213`

✅ **ロールバック**:
- 30秒で完全復元可能
- `./scripts/emergency_rollback.sh`

---

## 🎯 成功基準

### Phase2完了の定義

- [x] antigravity_adapter.py 実装完了
- [x] antigravity_adapter テスト全パス
- [x] claude_adapter.py 実装完了
- [x] claude_adapter テスト全パス
- [x] codex_adapter.py 実装完了
- [x] codex_adapter テスト全パス
- [x] 統合テスト全パス
- [x] 既存システムへの影響なし確認
- [x] Phase2完了レポート作成

### 品質目標

| 指標 | 目標 |
|------|------|
| テストカバレッジ | 100% |
| テスト成功率 | 100% |
| 破壊リスク | 0% |
| コード品質 | 優 |

---

## 📝 次のアクション

### 実装順序

1. **antigravity_adapter.py 実装** (1時間)
   - 基本クラス構造
   - 設定読み込み
   - トリガーワード処理

2. **antigravity_adapter テスト** (30分)
   - 12-15テストケース作成
   - 全テストパス確認

3. **claude_adapter.py 実装** (1.5時間)
   - 基本クラス構造
   - スキルシステム対応
   - デイリータスク対応

4. **claude_adapter テスト** (30分)
   - 15-18テストケース作成
   - 全テストパス確認

5. **codex_adapter.py 実装** (40分)
   - 基本クラス構造
   - スキルインデックス読み込み
   - .cursor参照処理

6. **codex_adapter テスト** (20分)
   - 10-12テストケース作成
   - 全テストパス確認

7. **統合テスト** (1時間)
   - エンドツーエンドテスト
   - クロスツールテスト
   - 全テストパス確認

8. **Phase2完了レポート** (30分)
   - 実装サマリー
   - テスト結果
   - 統計情報

---

## 💡 設計上の重要ポイント

### 1. 共通インターフェース

全アダプタは共通のインターフェースを実装:

```python
class BaseAdapter(ABC):
    """全アダプタの基底クラス"""

    @abstractmethod
    def load_config(self) -> Dict[str, Any]:
        """ツール固有の設定を読み込む"""
        pass

    @abstractmethod
    def process_trigger(self, trigger_word: str) -> str:
        """トリガーワードを処理"""
        pass

    @abstractmethod
    def generate_document(self, template_name: str, context: Dict) -> str:
        """文書を生成"""
        pass
```

### 2. 設定の読み込み方針

- **Antigravity**: `.antigravity/` と `.agent/` の両方を読み込み、マージ
- **Claude Code**: `.claude/` のみを読み込み
- **Codex**: `.codex/` を読み込み、`.cursor/` を参照

### 3. 既存コンポーネントの再利用

```python
class AntigravityAdapter(BaseAdapter):
    def __init__(self):
        # Phase1のコンポーネントを使用
        self.router = TriggerRouter()
        self.resolver = PathResolver()
        self.engine = TemplateEngine()

        # Antigravity固有の設定
        self.config = self.load_config()
```

---

## 🚀 Phase2開始準備完了

**破壊リスク**: 0%
**バックアップ**: ✅ 完備
**Phase1基盤**: ✅ 完成（51/51テストパス）
**開発環境**: ✅ 準備完了

Phase2の実装を開始します。

---

**Phase2開始日時**: 2025-12-30 19:30
**次のタスク**: antigravity_adapter.py 実装
**現在の状態**: 既存システム完全動作中、Phase1完璧に完了
**破壊リスク**: 0%
