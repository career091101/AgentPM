# プランC: ワークフロー保護最優先実行ガイド

**作成日**: 2025-12-30
**バックアップID**: pre-restructure-20251230_180213
**原則**: 既存ワークフローに一切影響を与えない

---

## 🛡️ 安全性最優先ルール

### 絶対厳守事項

1. **既存ファイルは絶対に削除・変更しない**
   - 全ての新規作成はコピーから開始
   - 既存フォルダは読み取り専用として扱う

2. **並行運用を必須とする**
   - 旧システム(v2): 現状のまま完全動作保証
   - 新システム(v3): 別環境で開発・テスト

3. **各ステップで動作確認**
   - 新規ファイル作成 → 即座にテスト
   - エラー検出 → 即座に削除・ロールバック

4. **進行中プロジェクトは保護**
   - `Stock/programs/創業支援・新規事業開発(AIエージェント)/` は触らない
   - `Flow/` の現在の作業も保護

---

## 📦 バックアップ情報

### 作成済みバックアップ

✅ **Git Tag**: `pre-restructure-20251230_180213`
✅ **バックアップ場所**: `backup/pre_restructure_20251230_180213/`
✅ **バックアップサイズ**: 1.9MB
✅ **バックアップ内容**:
- .agent/
- .antigravity/
- .claude/
- .codex/
- .cursor/
- docs/ai/

### 緊急ロールバック手順

```bash
# 即座に元に戻す（30秒）
git checkout pre-restructure-20251230_180213
# または
cp -r backup/pre_restructure_20251230_180213/.agent aipm_v0/
cp -r backup/pre_restructure_20251230_180213/.antigravity aipm_v0/
cp -r backup/pre_restructure_20251230_180213/.claude aipm_v0/
cp -r backup/pre_restructure_20251230_180213/.codex aipm_v0/
cp -r backup/pre_restructure_20251230_180213/.cursor aipm_v0/
cp -r backup/pre_restructure_20251230_180213/ai aipm_v0/docs/
```

---

## 🚀 安全な実行戦略

### Phase 0: 準備（破壊リスク: 0%）

#### ステップ1: 開発環境の分離

**目的**: 本番環境と完全に分離された開発環境を作成

**実装**:
```bash
# 開発用ディレクトリ作成
mkdir -p aipm_v0_dev/.cursor_v3/rules/basic
mkdir -p aipm_v0_dev/.agent_v3
mkdir -p aipm_v0_dev/.claude_v3
mkdir -p aipm_v0_dev/docs/ai/lib/core
mkdir -p aipm_v0_dev/docs/ai/lib/adapters
```

**破壊リスク**: 0% (既存ファイルに触れない)

---

#### ステップ2: 並行運用設定の作成

**config.yaml 方式** (全ツール共通):

```yaml
# aipm_v0/config.yaml (新規作成)

system:
  version: "v2"  # デフォルトは v2 (既存システム)

  # 並行運用モード
  parallel_mode:
    enabled: true
    v2_path: "aipm_v0/"        # 既存システム（現在動作中）
    v3_path: "aipm_v0_dev/"    # 新システム（開発中）

  # 安全モード
  safety:
    backup_before_switch: true
    rollback_on_error: true
    protect_stock: true        # Stock/ は変更不可
    protect_flow_current: true # Flow/の進行中作業を保護
```

**破壊リスク**: 0% (設定ファイル追加のみ)

---

#### ステップ3: Python開発環境構築（分離）

```bash
cd aipm_v0_dev

# Python仮想環境（本番とは別）
python3 -m venv venv_v3
source venv_v3/bin/activate

# 依存パッケージ
pip install pyyaml jinja2 pytest
```

**破壊リスク**: 0% (aipm_v0_dev/ 内のみ)

---

### Phase 1: SSOT層構築（破壊リスク: 0%）

**原則**: 全て aipm_v0_dev/ 内で作業

#### ステップ1.1: trigger_router.py 実装

**ファイル**: `aipm_v0_dev/docs/ai/lib/core/trigger_router.py`

```python
# 新規作成 - 既存ファイルには影響なし

import yaml
from pathlib import Path

class TriggerRouter:
    """v3用トリガーワードルーター"""

    def __init__(self, ssot_root="aipm_v0/.cursor/rules/basic"):
        # 既存の.cursorを参照（読み取り専用）
        self.ssot_root = Path(ssot_root)

    def route(self, trigger_word):
        # 実装...
        pass
```

**テスト**:
```bash
cd aipm_v0_dev
source venv_v3/bin/activate
python -m pytest tests/test_trigger_router.py -v
```

**破壊リスク**: 0% (新規ファイル、既存ファイル読み取りのみ)

---

#### ステップ1.2: 段階的テスト

**テスト方針**:
1. aipm_v0_dev/ で開発・テスト
2. 成功したら aipm_v0/ にコピー検討
3. コピー前に必ずバックアップ

---

### Phase 2: 移行判断ポイント

#### 判断基準

**Go判定** (aipm_v0/ への統合を許可):
- [ ] aipm_v0_dev/ で全テスト合格
- [ ] 既存ワークフローを壊さないことを確認
- [ ] ロールバックテスト成功
- [ ] ユーザー（あなた）の承認

**No-Go判定** (aipm_v0_dev/ 内で継続開発):
- 1つでも問題があれば本番環境には触れない

---

## 🔄 段階的統合戦略

### ステージ1: 開発環境のみ (Week 1-10)

**場所**: aipm_v0_dev/
**リスク**: 0%
**並行運用**: aipm_v0/ はそのまま

### ステージ2: カナリアデプロイ (Week 11-12)

**方法**:
1. aipm_v0_dev/ で完成
2. 1つの機能だけ aipm_v0/ にコピー
3. テスト
4. 問題なければ次の機能

**例**: タスク管理だけ先行統合
```bash
# バックアップ
cp aipm_v0/.cursor/rules/basic/07_task_management_ultralight.mdc \
   backup/07_task_management_ultralight.mdc.bak

# 新版コピー（試験的）
cp aipm_v0_dev/.cursor/rules/basic/07_task_management_v3.mdc \
   aipm_v0/.cursor/rules/basic/07_task_management_v3.mdc

# テスト
# 問題なし → 継続
# 問題あり → 即座にロールバック
```

### ステージ3: フルデプロイ (Week 13-18)

**条件**: カナリアデプロイで問題なし
**方法**: 段階的に全機能を統合

---

## 📊 並行運用期間中の動作

### ユーザー視点

**デフォルト**: v2（既存システム）を使用
```bash
# 何も設定しなければ v2 が動く
「プロジェクト憲章」 → aipm_v0/.cursor/rules/ 実行
```

**v3テスト時**: 明示的に切り替え
```bash
# 環境変数で切り替え
export AIPM_VERSION=v3

「プロジェクト憲章」 → aipm_v0_dev/.cursor_v3/rules/ 実行
```

**いつでもロールバック可能**:
```bash
# v2に戻す
export AIPM_VERSION=v2
# または環境変数を削除
unset AIPM_VERSION
```

---

## 🛠️ 実装の詳細ステップ

### Week 1-2: フェーズ0（準備）

#### Day 1: バックアップとディレクトリ構造

✅ **完了**:
- [x] Git tag作成
- [x] フルバックアップ作成
- [x] バックアップ検証

⏳ **次のステップ**:
```bash
# 開発環境ディレクトリ作成
cd /Users/yuichi/AIPM

mkdir -p aipm_v0_dev/.cursor_v3/rules/basic
mkdir -p aipm_v0_dev/.agent_v3/workflows
mkdir -p aipm_v0_dev/.claude_v3/skills
mkdir -p aipm_v0_dev/docs/ai/lib/core
mkdir -p aipm_v0_dev/docs/ai/lib/adapters
mkdir -p aipm_v0_dev/docs/ai/lib/templates
mkdir -p aipm_v0_dev/tests/unit
mkdir -p aipm_v0_dev/tests/integration
```

---

#### Day 2: Python環境構築

```bash
cd /Users/yuichi/AIPM/aipm_v0_dev

# Python仮想環境
python3 -m venv venv_v3
source venv_v3/bin/activate

# 依存パッケージ
pip install pyyaml jinja2 pytest pytest-cov

# requirements.txt作成
cat > requirements.txt <<'EOF'
pyyaml>=6.0
jinja2>=3.1.0
pytest>=7.0.0
pytest-cov>=4.0.0
EOF

pip freeze > requirements.txt.lock
```

---

#### Day 3-4: 並行運用設定

**config.yaml 作成**:
```yaml
# aipm_v0/config.yaml

system:
  version: "v2"

  parallel_mode:
    enabled: true
    v2_path: "aipm_v0/"
    v3_path: "aipm_v0_dev/"

  safety:
    backup_before_switch: true
    rollback_on_error: true
    protect_stock: true
    protect_flow_current: true

  # 保護対象パス
  protected_paths:
    - "Stock/programs/創業支援・新規事業開発(AIエージェント)/"
    - "Flow/202512/2025-12-30/"

  # ロールバック設定
  rollback:
    git_tag: "pre-restructure-20251230_180213"
    backup_dir: "backup/pre_restructure_20251230_180213/"
    auto_rollback_on_error: true
```

---

#### Day 5: ロールバックテスト

```bash
# テスト1: Git tagからのロールバック
cd /Users/yuichi/AIPM
git checkout pre-restructure-20251230_180213

# 確認
ls -la aipm_v0/.agent
ls -la aipm_v0/.cursor

# 元に戻す
git checkout main

# テスト2: バックアップからのロールバック
cp -r backup/pre_restructure_20251230_180213/.agent aipm_v0/.agent_test
diff -r aipm_v0/.agent aipm_v0/.agent_test

# クリーンアップ
rm -rf aipm_v0/.agent_test
```

**期待結果**: 30秒以内にロールバック完了

---

### Week 3-6: フェーズ1（SSOT層構築）

**場所**: 全て aipm_v0_dev/ 内
**既存システム**: 一切触れない

#### 実装ファイルリスト（全て新規作成）

```
aipm_v0_dev/
├── docs/ai/lib/
│   ├── core/
│   │   ├── trigger_router.py      (新規)
│   │   ├── path_resolver.py       (新規)
│   │   ├── template_engine.py     (新規)
│   │   └── validator.py           (新規)
│   ├── adapters/                  (新規)
│   └── templates/                 (新規)
├── .cursor_v3/rules/basic/
│   └── 07_task_management_v3.mdc  (新規)
├── tests/
│   ├── unit/                      (新規)
│   └── integration/               (新規)
└── venv_v3/                       (新規)
```

**破壊リスク**: 0% (全て新規ディレクトリ)

---

## 📝 チェックリスト

### フェーズ0完了条件

- [x] Git tag作成: `pre-restructure-20251230_180213`
- [x] バックアップ作成: `backup/pre_restructure_20251230_180213/`
- [ ] 開発環境ディレクトリ作成: `aipm_v0_dev/`
- [ ] Python仮想環境: `aipm_v0_dev/venv_v3/`
- [ ] config.yaml 作成
- [ ] ロールバックテスト成功
- [ ] ユーザー承認

### フェーズ1-5の開始条件

**必須**:
- フェーズ0完了
- 既存ワークフローが正常動作中
- ロールバック手順が確立

---

## 🚨 緊急時の対応

### 問題発生時のフロー

```
問題検出
    ↓
即座に作業停止
    ↓
影響範囲確認
    ↓
既存システムに影響あり?
    ↙        ↘
   Yes        No
    ↓          ↓
即座に      aipm_v0_dev/
ロール      内で修正
バック
```

### ロールバックコマンド（即座実行）

```bash
#!/bin/bash
# emergency_rollback.sh

echo "🚨 緊急ロールバック開始"

# Git tagに戻す
git checkout pre-restructure-20251230_180213

echo "✅ ロールバック完了"
echo "既存システムが復元されました"

# 動作確認
ls -la aipm_v0/.cursor/rules/basic/
```

---

## 📞 サポート

### 各ステップでの確認

毎ステップ完了時に以下を確認:
1. 既存ファイルに変更がないか
2. 既存ワークフローが動作するか
3. 新規ファイルのテストが通るか

### 不明点があれば即座に質問

- 「このステップは安全ですか?」
- 「既存ファイルに影響しますか?」
- 「ロールバックできますか?」

---

**作成者**: Claude Sonnet 4.5
**作成日**: 2025-12-30 18:02
**バックアップID**: pre-restructure-20251230_180213
**安全性レベル**: 最高
